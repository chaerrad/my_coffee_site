extends home.pug
block content
    h4  Login
    form(method="POST")
        input(placeholder="ID", tpye="text")
        input(placeholder="password", type="password")
        input(type="submit", value="submit")